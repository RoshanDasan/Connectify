import { Box } from "@mui/material";
import { styled } from "@mui/styles";


const WidgetWraper = styled(Box)(({}) =>({
    
    padding: '1.5rem 1.5rem 0.75rem 1.5rem',
    borderRadius: '0.75rem'
}) )


export default WidgetWraper;